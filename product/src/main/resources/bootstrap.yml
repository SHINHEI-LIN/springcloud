spring:
  application:
    name: product
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: dev
  rabbitmq:
    host: 111.230.169.249
    port: 5673

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/
